name: Test reposiotry_dispatch hook


on: repository_dispatch


jobs:
  release:
    runs-on: ubuntu-latest

    env:
      CI: true

    steps:
      - name: Does it work?
        run: |
          echo "Wooohoo"
